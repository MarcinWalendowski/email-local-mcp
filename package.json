{
  "name": "email-local-mcp",
  "version": "0.2.0",
  "type": "module",
  "description": "Email Local MCP — a local macOS MCP server that connects multiple email accounts to your AI agent: Gmail, iCloud, Fastmail, or any IMAP host (full CRUD over IMAP/SMTP, App Passwords in the Keychain).",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "gmail",
    "imap",
    "smtp",
    "email",
    "claude",
    "macos"
  ],
  "homepage": "https://github.com/MarcinWalendowski/email-local-mcp#readme",
  "bugs": "https://github.com/MarcinWalendowski/email-local-mcp/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/MarcinWalendowski/email-local-mcp.git"
  },
  "author": "Marcin Walendowski",
  "license": "MIT",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "bin": {
    "email-local-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "build:core": "tsc -p tsconfig.core.json",
    "pack:core": "npm run build:core && cp LICENSE core/LICENSE && npm pack ./core",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.core.json --noEmit && tsc -p tsconfig.test.json",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "surface": "tsx scripts/tool-surface.ts",
    "app:build": "scripts/build-app.sh",
    "app:dmg": "scripts/make-dmg.sh",
    "setup": "scripts/setup-cli.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@napi-rs/keyring": "^1.1.6",
    "imapflow": "^1.0.192",
    "mailparser": "^3.7.1",
    "nodemailer": "^9.0.3",
    "pino": "^9.5.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/mailparser": "^3.4.5",
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^6.4.16",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
